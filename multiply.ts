let Big = require('big.js');

/*function someFunc(n: number) {
    this.chain = function (n: number) {};
}*/

class someFunc {
    constructor(n: number) {

    }

    chain(n: number) {

    }
}

let x = 10 / 6 + 1 - 2 * 15;
let y = new someFunc(3).chain(2);

let z = 2;
//z = 'string';

console.log(x);